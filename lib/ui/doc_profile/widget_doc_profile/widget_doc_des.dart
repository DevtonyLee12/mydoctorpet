import 'package:flutter/material.dart';

class DocDes extends StatelessWidget {
  const DocDes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Container(
            height: 10,
          ),
          Container(
            child: Row(
              children: [],
            ),
          ),
        ],
      ),
    );
  }
}
